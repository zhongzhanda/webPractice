### 简单介绍：
几个块元素，当鼠标移进该元素时，该元素高度展开位最大高度，移出该元素时，该元素高度收缩。  
默认情况下，第一个元素展现为最大元素。  
  
### 简单分析
这道题很简单，主要是第一个元素默认情况下是展开状态。一开始以为是通过JS来控制的，后来看了原作的代码才发现一开始第一个元素默认展开最大高度，当鼠标移入ul时，第一个元素才和其他元素一样收缩起来。  
这里就不按照原作那样写标题和图片了。